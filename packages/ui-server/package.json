{
  "name": "@cortex-agent/ui-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Optional Web UI transport for Cortex: tRPC AppRouter binding + HTTP/SSE standalone host + same-origin SPA static serving. Loaded on demand by agent-server behind CORTEX_UI_HTTP; keeps @trpc/server out of the core (Slack/TUI-only) runtime.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "dependencies": {
    "@trpc/server": "^11",
    "jose": "^6",
    "zod": "^4.4.3",
    "@cortex-agent/server": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
