// input:  nothing (leaf data slice)
// output: schedulingEn / schedulingZh — message slice (filled by i18n extraction)
// pos:    one locale slice; aggregated by core/locales/en.ts & zh.ts barrels
// >>> Keep en and zh keys in lockstep (zh typed against keyof typeof schedulingEn) <<<

export const schedulingEn = {
  'schedule.help.title': '*Schedule commands:*',
  'schedule.help.addInterval': '`!schedule add interval <duration> [--profile <name>] <message>` — repeat every duration (e.g. `30m`, `2h`, `1d`)',
  'schedule.help.addDaily': '`!schedule add daily <HH:MM> [--profile <name>] <message>` — run every day at a fixed time (24-hour)',
  'schedule.help.addWeekly': '`!schedule add weekly <day> <HH:MM> [--profile <name>] <message>` — run every week (day: mon,tue,wed,thu,fri,sat,sun)',
  'schedule.help.addOnce': '`!schedule add once <duration> [--profile <name>] <message>` — run once after a delay',
  'schedule.help.list': '`!schedule list` — show all scheduled tasks',
  'schedule.help.pause': '`!schedule pause <id>` — pause a recurring task',
  'schedule.help.resume': '`!schedule resume <id>` — resume a paused recurring task',
  'schedule.help.remove': '`!schedule remove <id>` — remove a task by id',
  'schedule.help.availableProfiles': 'Available profiles: ${profileNames}',
  'schedule.list.empty': 'No scheduled tasks.',
  'schedule.list.header': '*Scheduled tasks (${count}):*\n${lines}',
  'schedule.remove.usage': 'Usage: `!schedule remove <id>`',
  'schedule.remove.ok': 'Removed task `${id}`.',
  'schedule.taskNotFound': 'Task not found: `${id}`',
  'schedule.pause.usage': 'Usage: `!schedule pause <id>`',
  'schedule.pause.ok': 'Paused task `${id}`.',
  'schedule.resume.usage': 'Usage: `!schedule resume <id>`',
  'schedule.resume.ok': 'Resumed task `${id}`.${nextAt}',
  'schedule.resume.nextRun': ' Next run: ${time}.',
  'schedule.invalidDuration': 'Invalid duration: `${duration}`. Use e.g. `30s`, `5m`, `2h`, `1d`',
  'schedule.invalidTime': 'Invalid time: `${time}`. Use 24-hour HH:MM, e.g. `${example}`',
  'schedule.invalidDay': 'Invalid day: `${day}`. Use: mon, tue, wed, thu, fri, sat, sun',
  'schedule.add.intervalUsage': 'Usage: `!schedule add interval <duration> [--profile <name>] <message>`',
  'schedule.add.dailyUsage': 'Usage: `!schedule add daily <HH:MM> [--profile <name>] <message>`',
  'schedule.add.weeklyUsage': 'Usage: `!schedule add weekly <day> <HH:MM> [--profile <name>] <message>`',
  'schedule.add.onceUsage': 'Usage: `!schedule add once <duration> [--profile <name>] <message>`',
  'schedule.add.scheduledInterval': 'Scheduled every *${duration}*: "${message}"',
  'schedule.add.scheduledDaily': 'Scheduled daily at *${time}*: "${message}"',
  'schedule.add.scheduledWeekly': 'Scheduled weekly *${day} ${time}*: "${message}"',
  'schedule.add.scheduledOnce': 'Scheduled once in *${duration}*: "${message}"',
  'schedule.add.unknownType': 'Unknown type: `${type}`. Use `interval`, `daily`, `weekly`, or `once`',
  'schedule.profileUsage': 'Usage: ${usage}',
} as const;

export const schedulingZh: Record<keyof typeof schedulingEn, string> = {
  'schedule.help.title': '*定时任务命令：*',
  'schedule.help.addInterval': '`!schedule add interval <duration> [--profile <name>] <message>` — 每隔指定时长重复（例如 `30m`、`2h`、`1d`）',
  'schedule.help.addDaily': '`!schedule add daily <HH:MM> [--profile <name>] <message>` — 每天在固定时间运行（24 小时制）',
  'schedule.help.addWeekly': '`!schedule add weekly <day> <HH:MM> [--profile <name>] <message>` — 每周运行（day：mon,tue,wed,thu,fri,sat,sun）',
  'schedule.help.addOnce': '`!schedule add once <duration> [--profile <name>] <message>` — 延迟一段时间后运行一次',
  'schedule.help.list': '`!schedule list` — 显示所有定时任务',
  'schedule.help.pause': '`!schedule pause <id>` — 暂停一个周期任务',
  'schedule.help.resume': '`!schedule resume <id>` — 恢复一个已暂停的周期任务',
  'schedule.help.remove': '`!schedule remove <id>` — 按 id 删除任务',
  'schedule.help.availableProfiles': '可用的 profile：${profileNames}',
  'schedule.list.empty': '没有定时任务。',
  'schedule.list.header': '*定时任务（${count}）：*\n${lines}',
  'schedule.remove.usage': '用法：`!schedule remove <id>`',
  'schedule.remove.ok': '已删除任务 `${id}`。',
  'schedule.taskNotFound': '未找到任务：`${id}`',
  'schedule.pause.usage': '用法：`!schedule pause <id>`',
  'schedule.pause.ok': '已暂停任务 `${id}`。',
  'schedule.resume.usage': '用法：`!schedule resume <id>`',
  'schedule.resume.ok': '已恢复任务 `${id}`。${nextAt}',
  'schedule.resume.nextRun': ' 下次运行：${time}。',
  'schedule.invalidDuration': '无效的时长：`${duration}`。请使用例如 `30s`、`5m`、`2h`、`1d`',
  'schedule.invalidTime': '无效的时间：`${time}`。请使用 24 小时制 HH:MM，例如 `${example}`',
  'schedule.invalidDay': '无效的星期：`${day}`。请使用：mon, tue, wed, thu, fri, sat, sun',
  'schedule.add.intervalUsage': '用法：`!schedule add interval <duration> [--profile <name>] <message>`',
  'schedule.add.dailyUsage': '用法：`!schedule add daily <HH:MM> [--profile <name>] <message>`',
  'schedule.add.weeklyUsage': '用法：`!schedule add weekly <day> <HH:MM> [--profile <name>] <message>`',
  'schedule.add.onceUsage': '用法：`!schedule add once <duration> [--profile <name>] <message>`',
  'schedule.add.scheduledInterval': '已设定每 *${duration}* 运行一次："${message}"',
  'schedule.add.scheduledDaily': '已设定每天 *${time}* 运行："${message}"',
  'schedule.add.scheduledWeekly': '已设定每周 *${day} ${time}* 运行："${message}"',
  'schedule.add.scheduledOnce': '已设定 *${duration}* 后运行一次："${message}"',
  'schedule.add.unknownType': '未知的类型：`${type}`。请使用 `interval`、`daily`、`weekly` 或 `once`',
  'schedule.profileUsage': '用法：${usage}',
};
