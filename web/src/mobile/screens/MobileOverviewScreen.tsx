// STUB slot — design 10f 移动端项目 Overview (6a 单列压缩). Reached from the shell header. Replaced by
// a later pass.
import { useVocab } from '@/i18n';
import { StubScreen } from './StubScreen';

export function MobileOverviewScreen() {
  const v = useVocab();
  return <StubScreen screenId="10f" title={v.overview} />;
}
