// STUB slot — design 5b 移动端线程 (线程 tab). Replaced by a later pass.
import { useVocab } from '@/i18n';
import { StubScreen } from './StubScreen';

export function MobileThreadsScreen() {
  const v = useVocab();
  return <StubScreen screenId="5b" title={v.threads} />;
}
