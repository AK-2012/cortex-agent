{
  "name": "@cortex-agent/desktop",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "copy-connect": "mkdir -p ../web/dist && cp -f ui/connect.html ../web/dist/connect.html",
    "dev": "npm run copy-connect && tauri dev",
    "build": "npm run copy-connect && tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
